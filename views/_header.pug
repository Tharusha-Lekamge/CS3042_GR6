header.header 
  // Navigation Bar
  nav.nav.nav--home
    .container 
      .logo 
        a.nav__el(href='/') MicroBank
  nav.nav.nav--user 
    if user 
      a.nav__el(href='#')= `Welcome ${user.customerID}`
      a.nav__el(href=`/account-overview?id=${user.customerID}`) My Account 
      a.nav__el(href='#') Fixed Deposits
      a.nav__el.nav__el--logout Log out
    else 
      a.nav__el(href="/login") LogIn 
      a.nav__el(href='/signup') SignUp